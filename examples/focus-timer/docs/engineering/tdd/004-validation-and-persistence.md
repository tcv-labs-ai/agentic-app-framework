# TDD 004: Validation and Persistence

## Direction

- validate duration inputs in the application layer before applying them
- persist configured durations in `localStorage`
- keep invalid input feedback visible near the settings form

## Acceptance Criteria

- valid durations persist across reloads
